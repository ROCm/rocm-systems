.. Generated by ROCm docgen prompt_version=2026-06-02-v15-wiki-pm-exclusion-and-install-fix at 2026-06-03T04:12:44Z

.. meta::
<<<<<<< HEAD:projects/hipfile/docs/conceptual/io-backends.rst
   :description: IO backend architecture in hipFile, covering the fastpath and fallback backends, scoring model, automatic fallback behavior, and compatibility mode controls.
   :keywords: hipFile, ROCm, IO backend, fastpath, fallback, O_DIRECT, DMA, GPU IO, compat mode, AMD

.. provenance:
..   mode: NEW
..   sources:
..     - src/amd_detail/backend.h
..     - src/amd_detail/backend/fastpath.h
..     - src/amd_detail/backend/fallback.h
..     - src/amd_detail/environment.h
..     - src/amd_detail/configuration.h
..     - src/amd_detail/file.h
..     - include/hipfile.h
..     - examples/common/examples_common.h
..     - examples/basics/no-odirect-write.cpp
..     - examples/aiscp/aiscp.cpp
..     - src/amd_detail/backend/memcpy-kernel.h
..     - src/amd_detail/hip.h
..     - src/amd_detail/backend/fallback.cpp
=======
  :description: Describes the hipFile I/O backend architecture, including fastpath and fallback backend selection, scoring, automatic retry, and environment variable configuration.
  :keywords: hipFile, backend, fastpath, fallback, score, POSIX, direct-to-GPU, ROCm, I/O, HIPFILE_ALLOW_COMPAT_MODE, HIPFILE_FORCE_COMPAT_MODE
>>>>>>> 8dd6d433e4 (applied style guide; updated with review feedback; reorganized and edited):projects/hipfile/docs/reference/io-backends.rst

=======================
I/O backend architecture
=======================

<<<<<<< HEAD:projects/hipfile/docs/conceptual/io-backends.rst
hipFile uses a two-backend design for every read and write operation. At IO submission time, the library evaluates each backend's willingness to service the request and dispatches work to the most suitable one. If the preferred backend encounters a transient error, the request can be retried automatically on the other backend. This page describes both backends, the scoring model that selects between them, and the environment variables that control fallback behavior.

.. mermaid::

   flowchart LR
       A["hipFileRead / hipFileWrite"] --> B["Score backends"]
       B --> C{"Fastpath score >= 0?"}
       C -- Yes --> D["Fastpath IO\n(HIP runtime → KFD)"]
       C -- No --> F["Fallback IO\n(pread/pwrite + hipMemcpy)"]
       D -- "ENODEV / EREMOTEIO" --> F
=======
hipFile routes each I/O request through a multi-backend stack on AMD platforms. The library selects the backend with the highest score for the request and can retry a failed operation on another backend.

Backend model
=============

hipFile defines a ``Backend`` base class that every I/O path implements. Two concrete backends exist for the AMD platform:

Fastpath
   Uses the HIP runtime's direct I/O functions, ``hipAmdFileRead`` and ``hipAmdFileWrite``, to transfer data directly between storage and GPU memory without a CPU bounce buffer.

Fallback
   Uses POSIX ``pread`` and ``pwrite`` with a host-memory bounce buffer to move data between storage and GPU memory.

When you submit an I/O request through ``hipFileRead`` or ``hipFileWrite``, hipFile evaluates every registered backend and selects the one best suited to the request.

Backend scoring
===============

Each backend implements a ``score()`` method that returns an integer. The integer reflects how eager the backend is to handle a given I/O request. The scoring rules are:

- A non-negative return value means the backend accepts the request. A higher value means greater eagerness.
- A negative return value means the backend refuses the request. If hipFile routed the request to that backend, the operation would likely fail.

hipFile iterates over all available backends, calls ``score()`` on each, and selects the backend with the highest non-negative score. You can add or reorder backends without changing the dispatch logic.

.. code-block:: cpp

   /// Indicates the willingness of the backend to service an I/O
   /// request. The higher the number the more eager the backend is
   /// to service the request. If a negative number is returned,
   /// the backend is refusing to perform the operation.
   virtual int score(std::shared_ptr<IFile> file,
                     std::shared_ptr<IBuffer> buffer,
                     size_t size,
                     hoff_t file_offset,
                     hoff_t buffer_offset) const = 0;

Fastpath eligibility
====================

The fastpath backend returns a positive score and is selected when all of the following conditions are met:

1. HIP runtime symbols are present. At initialization, hipFile looks up ``hipAmdFileRead`` and ``hipAmdFileWrite`` in the HIP runtime library. If either symbol is missing, the fastpath backend is disabled.

2. Force-compat mode isn't set. If you set ``HIPFILE_FORCE_COMPAT_MODE`` to ``true``, the configuration layer disables the fastpath backend regardless of symbol availability.

3. The target file type is eligible. The file must be a block device or a regular file, as reported by ``statx``.

4. The file system is supported. The file must reside on one of the following file systems:

   - ext4 with ordered journaling mode
   - xfs

   hipFile reads ``/proc/self/mountinfo`` through ``libmount`` to determine the file system type and mount options. You can skip this check by setting ``HIPFILE_UNSUPPORTED_FILE_SYSTEMS=true``. That setting lets the fastpath operate on file systems that haven't been validated.

For file system requirements, see :doc:`/conceptual/filesystem-requirements`.

Fallback path
=============

The fallback backend uses POSIX ``pread`` and ``pwrite`` to transfer data between storage and a host-memory bounce buffer. It then copies data to or from GPU memory with HIP memory copy operations. The fallback backend accepts most requests and returns a lower score than the fastpath. hipFile selects it only when the fastpath is unavailable or refuses the request.

You can disable the fallback backend by setting ``HIPFILE_ALLOW_COMPAT_MODE=false``. When the fallback is disabled, I/O requests that the fastpath can't handle fail instead of falling back to POSIX I/O.

Automatic retry with BackendWithFallback
========================================

The fastpath backend extends ``BackendWithFallback``, a ``Backend`` specialization that adds automatic retry logic. When a fastpath I/O operation fails, ``BackendWithFallback`` checks whether the request can retry on a registered fallback backend.

.. mermaid::

   flowchart TD
       A[I/O request submitted] --> B[Score all backends]
       B --> C{Highest score >= 0?}
       C -- Yes --> D[Execute I/O on selected backend]
       D --> E{I/O succeeded?}
       E -- Yes --> F[Return bytes transferred]
       E -- No --> G{Fallback eligible?}
       G -- Yes --> H[Retry I/O on fallback backend]
       H --> F
       G -- No --> I[Return error]
       C -- No --> I
>>>>>>> 8dd6d433e4 (applied style guide; updated with review feedback; reorganized and edited):projects/hipfile/docs/reference/io-backends.rst

Fastpath backend
****************

<<<<<<< HEAD:projects/hipfile/docs/conceptual/io-backends.rst
The fastpath backend transfers data directly between storage and GPU memory through the HIP runtime, the ROCR runtime, HSA, and the KFD kernel driver. Because the kernel driver performs DMA on behalf of the GPU, no CPU staging buffer is involved and the data does not pass through the kernel page cache.
=======
1. The fastpath backend's ``_io_impl`` attempts the I/O operation on the HIP runtime direct I/O path.
2. If the operation throws an exception or otherwise fails, ``BackendWithFallback::io()`` calls ``is_fallback_eligible()`` to check whether the fallback path can recover from the error.
3. If the request is eligible and a fallback backend is registered through ``register_fallback_backend()``, hipFile reissues the same I/O operation to the fallback backend.
4. If the request isn't eligible for fallback, the original error propagates to the caller.
>>>>>>> 8dd6d433e4 (applied style guide; updated with review feedback; reorganized and edited):projects/hipfile/docs/reference/io-backends.rst

Requirements
------------

The fastpath backend's ``score()`` function checks a set of preconditions before accepting a request. If any check fails, the function returns a negative score (refusing the request). The checks include:

File descriptor
   The file must have been opened with ``O_DIRECT``. hipFile internally maintains both a buffered and an unbuffered file descriptor for each registered file and uses the unbuffered descriptor for fastpath IO. If no unbuffered descriptor is available, the fastpath refuses the request.

Memory type
   The destination or source buffer must be ``hipMemoryTypeDevice`` memory. Only device-resident buffers can participate in the direct DMA path.

Filesystem
   The file must reside on a block device, or on a regular file stored on an **ext4** filesystem with ordered journaling mode, or on an **xfs** filesystem. Files on other filesystems are rejected by the fastpath unless the ``HIPFILE_UNSUPPORTED_FILE_SYSTEMS`` environment variable is set to ``true``.

Alignment
   File offsets, buffer offsets, and IO sizes must be aligned to the filesystem's direct IO alignment. hipFile queries this alignment from ``statx`` using ``STATX_DIOALIGN``; if the information is not available, it defaults to **4096 bytes**. The ``BLOCK_ALIGN`` constant used in the example programs is likewise defined as ``4096``.

IO size limit
   A single IO operation transfers at most ``0x7ffff000`` (2,147,479,552) bytes, matching the Linux kernel's ``MAX_RW_COUNT`` limit. ``getMaxRwCount()`` in the library computes this value as ``INT_MAX & ~(page_size - 1)``. Larger requests are capped to this size. The ``aiscp`` example defines ``AISCP_CHUNK_SIZE`` to the same value and loops over multiple calls for files that exceed it.

Fallback backend
****************

The fallback backend uses POSIX ``pread`` and ``pwrite`` system calls combined with ``hipMemcpy`` to bounce data through a host-memory buffer. Because it does not require ``O_DIRECT``, specific filesystem types, or aligned offsets, it can service any IO request that the fastpath cannot.

For synchronous IO, the fallback performs:

1. A ``pread`` or ``pwrite`` between the file and a host bounce buffer.
2. A ``hipMemcpy`` between the host bounce buffer and the GPU device buffer.

For asynchronous IO, the fallback path uses ``hipLaunchHostFunc`` and the ``hipFileMemcpyKernel`` HIP kernel to interleave CPU-side IO and GPU memory copies on the stream, so that work proceeds without blocking the calling thread.

Backend scoring model
*********************

Each backend implements a ``score()`` method that returns an integer:

- A **negative** return value means the backend refuses the request.
- A **non-negative** return value means the backend is willing. Higher values indicate greater eagerness.

When hipFile dispatches an IO operation, it queries every registered backend and selects the one with the highest non-negative score. In practice, the fastpath returns a positive score when all of its preconditions are met and a negative score otherwise. The fallback backend accepts any request, so it acts as the catch-all.

Automatic fallback with ``BackendWithFallback``
***********************************************

The fastpath backend extends ``BackendWithFallback``, a wrapper that intercepts errors from a primary backend and optionally retries the IO on a registered fallback backend. After the primary backend's ``_io_impl`` returns or throws, ``BackendWithFallback`` calls ``is_fallback_eligible()`` to decide whether to retry:

- If the primary backend reports ``ENODEV`` or ``EREMOTEIO``, the request is eligible for automatic fallback.
- If fallback is eligible and a fallback backend has been registered (via ``register_fallback_backend``), the request is resubmitted to that backend transparently.

This means that a request can initially target the fastpath and still complete through the POSIX path without the caller needing to handle the retry.

Compatibility mode controls
***************************

Two environment variables govern whether the fallback path is available:

``HIPFILE_ALLOW_COMPAT_MODE``
<<<<<<< HEAD:projects/hipfile/docs/conceptual/io-backends.rst
   Controls whether IO operations can fall back to the POSIX path when the fastpath cannot complete them. When set to ``true`` (the default behavior), fallback is permitted. When set to ``false``, operations that cannot be completed by the fastpath are rejected. The ``no-odirect-write`` example demonstrates this variable: it opens a file without ``O_DIRECT`` and requires ``HIPFILE_ALLOW_COMPAT_MODE=1`` to succeed.

``HIPFILE_FORCE_COMPAT_MODE``
   When set to ``true``, the fastpath is disabled entirely and all IO is routed through the POSIX fallback path. When set to ``false`` (the default behavior), the fastpath is used when its preconditions are met.
=======
   Controls whether the POSIX fallback backend is available. When set to ``false``, the fallback backend is disabled and I/O requests that the fastpath can't handle fail. The default is ``true``, so the fallback is allowed.

``HIPFILE_FORCE_COMPAT_MODE``
   Forces all I/O through the POSIX fallback path and bypasses the fastpath. When set to ``true``, the fastpath backend is disabled regardless of whether HIP runtime direct I/O symbols are available. The default is ``false``.
>>>>>>> 8dd6d433e4 (applied style guide; updated with review feedback; reorganized and edited):projects/hipfile/docs/reference/io-backends.rst

These variables are read from the process environment at library initialization through the ``Environment`` class and are reflected in the ``Configuration`` class methods ``fastpath()`` and ``fallback()``.

<<<<<<< HEAD:projects/hipfile/docs/conceptual/io-backends.rst
For a full list of environment variables and their accepted values, see :doc:`/conceptual/environment-variables`. For details on driver initialization and the reference-counted lifecycle, see :doc:`/reference/api-driver-lifecycle`.
=======
.. list-table::
   :header-rows: 1
   :widths: 30 30 40

   * - ``HIPFILE_FORCE_COMPAT_MODE``
     - ``HIPFILE_ALLOW_COMPAT_MODE``
     - Behavior
   * - ``false`` (default)
     - ``true`` (default)
     - Fastpath preferred, fallback available on failure
   * - ``false``
     - ``false``
     - Fastpath only, no fallback on failure
   * - ``true``
     - ``true`` (default)
     - Fallback only; fastpath disabled
   * - ``true``
     - ``false``
     - Both backends disabled; I/O fails

For a complete list of environment variables and their default values, see :doc:`/reference/environment-variables`.
>>>>>>> 8dd6d433e4 (applied style guide; updated with review feedback; reorganized and edited):projects/hipfile/docs/reference/io-backends.rst
