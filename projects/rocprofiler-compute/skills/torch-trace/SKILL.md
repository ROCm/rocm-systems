---
name: torch-trace
description: Attributes AMD GPU kernel performance counters back to the PyTorch or Triton operators that launched them, using rocprof-compute torch and triton tracing. Use when the user asks which PyTorch layer or operator is slow, wants to map GPU kernels to model code, profiles a training or inference script, or asks about torch.compile and Inductor kernels. Not for CUDA tools, Windows, non-Python applications, or user-authored ROCTx ranges.
---

# Map GPU kernels to PyTorch operators

Profiling a PyTorch model with counters alone gives kernel names, which say
little about the model. Torch tracing annotates the run so each kernel can be
traced back to the operator that launched it.

Torch and Triton tracing are experimental. Every command needs
`--experimental`, and option names can change between releases, so run
`rocprof-compute profile --experimental --help` first.
Never use the GUI or TUI.

## 1. Check the requirements

Torch tracing needs PyTorch 2.13 or 2.14, installed against a matching ROCm.
The workload must be a Python command or Python script; the tracer cannot
annotate a compiled binary. The full requirement list, including the Python
version matching constraint, is in the Torch trace section of
[profile mode](../../docs/how-to/profile/mode.rst).

Confirm the PyTorch version before profiling. A mismatched version fails at
collection time, after the run has already cost the user its runtime.

## 2. Collect

```bash
rocprof-compute profile \
    --experimental \
    --torch-trace \
    --name <workload_name> \
    -- python <script.py> <args>
```

For Triton kernels, including those generated by `torch.compile` and Inductor:

```bash
rocprof-compute profile --experimental --triton-trace --name <name> -- python <script.py>
```

The two combine, and `--ml-api-trace` turns on every supported framework
backend at once.

Tracing adds overhead on top of the usual counter replay. Profile a few
iterations of a steady-state model rather than a full training run, and skip
warmup. `ROCPROFCOMPUTE_ROCTX_DEEP_TENSOR_WRAPS=0` reduces instrumentation
overhead by dropping the device and layout wraps; see
[profile mode](../../docs/how-to/profile/mode.rst) for what that costs you.

Tracing cannot be combined with attaching to a running process, and cannot be
combined with a PC-sampling-only profile.

When counters are being spread thin across many short operator kernels,
`--iteration-multiplexing kernel` collects more per pass.

## 3. List the captured operators

Always list before filtering. Operator names come from the model that ran, so
guessing a pattern wastes a profile:

```bash
rocprof-compute analyze \
    --path ./workloads/<name>/<gpu_model> \
    --experimental --list-torch-operators
```

This prints the operators as a call tree grouped by source location, with
kernel launch statistics, so it also shows which operators are worth opening.

The Triton equivalent is `--list-triton-operators`.

## 4. Filter to an operator

```bash
rocprof-compute analyze \
    --path ./workloads/<name>/<gpu_model> \
    --experimental --torch-operator '*conv2d*'
```

Patterns are shell-style globs, not regexes, matched against a `/`-separated
operator hierarchy. `*relu` ends with relu, `*conv*` contains conv,
`*/*functional*/*` matches an intermediate component, and `all` or `'*'`
matches everything. Several patterns can be given, space or comma separated.
Quote patterns so the shell does not expand them.

Combine with `-k` to intersect an operator with specific kernel ids, and with
`-b` to look at one metric block for that operator's kernels. `--triton-operator`
works the same way.

Worked examples are in the PyTorch operator analysis section of
[the analyze CLI guide](../../docs/how-to/analyze/cli.rst).

## 5. Read the result

Attribution tells you which operator owns the kernel time. It does not tell you
why that kernel is slow. Once an operator is identified, analyze its kernels
like any other: Speed-of-Light first, then the memory or roofline path.

An operator that is slow because it launches thousands of tiny kernels is a
different problem from one slow kernel, and the launch statistics in the
operator list are what distinguish the two.

## 6. ROCTx scope

rocprof-compute uses ROCTx internally: torch tracing injects ROCTx regions to
annotate operators, which is what makes attribution possible. That is the only
place ROCTx is involved.

rocprof-compute does not support ROCTx-range-based profiling control, such as
starting, stopping, or resuming collection at a user-authored ROCTx range, and
does not attribute results to ranges a user added to their own application. If
that is what the user wants, say so plainly and offer kernel filtering with
`-k` or dispatch filtering with `-d` instead.
