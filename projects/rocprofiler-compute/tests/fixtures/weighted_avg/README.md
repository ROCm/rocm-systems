# WEIGHTED_AVG pilot fixture (Phase 2)

Contract example for `WEIGHTED_AVG` parent metrics until a gfx942 metric is
classified **SLOT_LIMIT** in Phase 1 worksheets. Do not ship this fragment in
production `analysis_configs/` without reviewer sign-off.

See `pilot_metric_table.yaml` and `docs/plans/aiprofcomp-865-phase2-weighted-avg-design.md`.
