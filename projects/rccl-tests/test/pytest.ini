[pytest]
addopts = -v --tb=short
markers =
    mpi: tests requiring MPI runtime
    regression: full regression sweep (datatypes × byte ranges × GPU counts)
    gfx942_gfx950: tests requiring gfx942 or gfx950 GPU architecture
