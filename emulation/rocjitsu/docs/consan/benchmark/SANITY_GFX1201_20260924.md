# RDNA4 sanity check after atomic publication changes

September 24, 2026, gfx1201 / Radeon RX 9070. This is a targeted regression
check, not a replacement benchmark campaign. The historical timing table stays
unchanged. Its `/tmp/consan-benchmark-gfx1201-refresh-fixed` artifacts no longer
exist, so comparisons with that table cannot isolate source changes from changes
in the ROCm/PyTorch stack or measurement variation.

## Regression found and fixed

The Gluon shared-memory round trip passed natively but failed under Default
with `HSA_STATUS_ERROR_ILLEGAL_INSTRUCTION`. Its final ISA retained an
`s_clause` covering ordinary global stores while the new publication capture
inserted arithmetic into the clause. That path has no synchronization sequence,
so it bypassed the sequence-based clause rewrite.

Commit `ac6019b08d6` looks up the enclosing clause from the decoded control flow
for every captured modification. It reuses the existing deduplicated NOP rewrite.
The Gluon workload subsequently passed Default, high, and SuperCollider, with
complete static and dynamic coverage and a passing numerical oracle. A new
lowering test covers two ordinary stores in one clause without a release/acquire
sequence. The ConSan suite passed 975 tests; two opt-in benchmark tests skipped.

## Performance checks

All checks used the benchmark runner's native rocprofv3 discovery and generated
exact kernel allowlists, numerical oracles, static coverage audit, and final
native drift check. GPU workloads ran serially. Detailed measurements and the
validation results are retained in the artifact root below.

Initial dense decode: native 2.72 ms (historical 2.67 ms); Default 5.79 ms
(7.04 ms); high 13.3 ms (10.2 ms); SuperCollider 5.03 ms (3.87 ms).
This initial run preceded the clause fix. Startup was 86.6/86.7/56.1 seconds
versus the historical 198/198/155 seconds.

After the fix, Gluon: native 94.2 µs (180 µs); Default 290 µs (217 µs);
high 264 µs (198 µs); SuperCollider 159 µs (175 µs). Startup stayed around
0.46 seconds. The Default latency increase is visible even though the absolute
cost remains below 0.3 ms; changed native latency makes the slowdown ratios
particularly unsuitable for attributing a regression to these commits.

After the fix, hipBLASLt: native 12.8 µs (9.48 µs); Default 110 µs (89.5 µs);
high 763 µs (733 µs); SuperCollider 37.9 µs (26.0 µs). Startup was
0.625/0.605/0.436 seconds versus 0.897/0.886/0.570 seconds. The client requires
its matching `therock_venv` libraries and profiler: using the newer validation
ROCm libraries failed native discovery with an incompatible Origami symbol.

The initial decode, fixed Gluon, and matched hipBLASLt runs had final native
Run drift of +3.2%, +1.9%, and +1.8%, respectively. All nine admitted
instrumented cells reported complete coverage and zero conflicts.

A fresh full decode rerun on the fixed hook passed all numerical and coverage
checks but **failed the manual timing-admission check**: final native Run latency
rose from 2.73 ms to 5.72 ms (+110%). Its 6.55/11.9/6.23 ms instrumented
measurements are retained only as diagnostics and cannot establish a performance
change. The runner records drift but does not automatically reject it; reading
the final native comparison is necessary before admitting timing conclusions.

The first campaign's high and SuperCollider decode timings were about 30%
slower than the historical table, with acceptable contemporaneous native drift.
This is a noticeable increase, not evidence of unchanged performance. Without
the old executable and environment provenance, this sanity check cannot attribute
it to the publication work. None of the admitted sampled runs showed a multi-fold
absolute-latency or startup regression. The historical table is left intact.

A final bounded SuperCollider check used the same fixed hook, command, inputs,
allowlist, and acceptance checks, bracketed by two fresh native processes on
each side. SuperCollider took 3.49 ms (historical 3.87 ms), passed numerical and
complete-coverage checks, and did not reproduce the earlier slowdown. Native
samples were 2.87/2.46 ms before and 3.84/2.69 ms after: host timing remains
variable, so this does not establish a precise speedup or a stable slowdown
ratio. Together these checks support similar performance scale, with insufficient
evidence to attribute the observed timing differences to the source changes.

## Correctness revalidation

The fixed hook preserved both selected Default-engine qualifications:

| Workload | Preset | Strict clean | Fault trials | Coverage / health |
| --- | --- | --- | --- | --- |
| Production FP16 matmul | high | Pass | 8/8 detected | Complete; all checks healthy |
| Qwen3-0.6B prefill | higher | Pass | 8/8 detected | Complete; all checks healthy |

These reused the current workload-specific rocprofv3 allowlists with unchanged
workloads, inputs, and runtime stacks. The reviewed barrier-drop mutations
reached the detector in all 16 trials. Numerical correctness alone was not the
acceptance criterion: clean runs required no diagnostics, and fault runs required
complete evidence and at least 6/8 detections. `STATUS_RDNA4.md` retains both
green cells with links to this recheck.

## Artifacts

Local root:
`/home/benoit/workspace/consan-validation/post-publication-sanity-20260924/`.

- `benchmark/`: initial decode and failed Gluon; the failure is retained.
- `gluon-dump/`, `gluon-patched.s`, `gluon-dump.log`: reproducer and faulty ISA.
- `benchmark-fixed/`: passing Gluon after the clause fix.
- `benchmark-decode-repeat/`: fixed-hook decode; timing rejected for native drift.
- `decode-sc-bracket/`, `repeat_sc.py`: bounded SuperCollider recheck with
  commands, controls, payloads, coverage, hook hash, and bracketing native runs.
- `benchmark-gemm-matched/`: passing hipBLASLt with its matching ROCm stack.
- `clause-tests.log`: full ConSan test results.
- `validation/`: strict clean runs and fault-injection trials.
- `validation-audit.json`: all 16 trials detected, complete, healthy, and using
  the same fixed hook before and after execution.

Initial hook SHA256:
`5e1307bcb88cd4944522b2ea699376b8963b912040b8e7a73eb6d83c4686da61`.
Fixed hook SHA256:
`ba3b0e45f5b441752d8f8f437485de835f86620355cfbe49face2678b024fc05`.
