// Copyright (c) 2025-2026 Advanced Micro Devices, Inc.
// SPDX-License-Identifier: MIT
//
// AUTO-GENERATED by the amdisa codegen pipeline. DO NOT EDIT.
// See lib/python/amdisa/README.md for regeneration instructions.

#include "rocjitsu/isa/arch/amdgpu/generated/rdna3/mtbuf.h"
#include "rocjitsu/isa/arch/amdgpu/rdna3/addr_calc.h"
#include "rocjitsu/isa/arch/amdgpu/shared/gfx11_cache_flags.h"
#include "rocjitsu/isa/arch/amdgpu/shared/scalar_operand_read.h"
#include "rocjitsu/isa/arch/amdgpu/shared/simd_glue.h"
#include "rocjitsu/vm/amdgpu/buffer_format.h"
#include "rocjitsu/vm/amdgpu/compute_unit.h"
#include "rocjitsu/vm/amdgpu/mem_state.h"
#include "rocjitsu/vm/amdgpu/wavefront.h"
#include "util/data_types.h"
#include "util/except.h"
#include <algorithm>
#include <bit>
#include <cmath>
#include <cstring>
#include <limits>
#include <memory>

namespace rocjitsu {
namespace rdna3 {

void TbufferLoadFormatXMtbuf::execute_impl(amdgpu::Wavefront &wf) {
  auto d = std::make_unique<amdgpu::VectorMemState>(amdgpu::GLOBAL_MEM);
  d->is_load = true;
  d->mtype = amdgpu::mtype_from_flags_gfx11(inst_.glc, inst_.dlc, inst_.slc);
  d->non_temporal = 0;
  d->wait_counter_type = amdgpu::WaitCounterType::LOADCNT;
  d->dst_reg_base = wf.vgpr_alloc().base + 0u + inst_.vdata;
  if (amdgpu::prepare_buffer_format(wf, *d, inst_.srsrc * 4, inst_.format, 1))
    mtbuf_calculate_addresses(inst_, wf, *d);
  set_data(std::move(d));
}

void TbufferLoadFormatXyMtbuf::execute_impl(amdgpu::Wavefront &wf) {
  auto d = std::make_unique<amdgpu::VectorMemState>(amdgpu::GLOBAL_MEM);
  d->is_load = true;
  d->mtype = amdgpu::mtype_from_flags_gfx11(inst_.glc, inst_.dlc, inst_.slc);
  d->non_temporal = 0;
  d->wait_counter_type = amdgpu::WaitCounterType::LOADCNT;
  d->dst_reg_base = wf.vgpr_alloc().base + 0u + inst_.vdata;
  if (amdgpu::prepare_buffer_format(wf, *d, inst_.srsrc * 4, inst_.format, 2))
    mtbuf_calculate_addresses(inst_, wf, *d);
  set_data(std::move(d));
}

void TbufferLoadFormatXyzMtbuf::execute_impl(amdgpu::Wavefront &wf) {
  auto d = std::make_unique<amdgpu::VectorMemState>(amdgpu::GLOBAL_MEM);
  d->is_load = true;
  d->mtype = amdgpu::mtype_from_flags_gfx11(inst_.glc, inst_.dlc, inst_.slc);
  d->non_temporal = 0;
  d->wait_counter_type = amdgpu::WaitCounterType::LOADCNT;
  d->dst_reg_base = wf.vgpr_alloc().base + 0u + inst_.vdata;
  if (amdgpu::prepare_buffer_format(wf, *d, inst_.srsrc * 4, inst_.format, 3))
    mtbuf_calculate_addresses(inst_, wf, *d);
  set_data(std::move(d));
}

void TbufferLoadFormatXyzwMtbuf::execute_impl(amdgpu::Wavefront &wf) {
  auto d = std::make_unique<amdgpu::VectorMemState>(amdgpu::GLOBAL_MEM);
  d->is_load = true;
  d->mtype = amdgpu::mtype_from_flags_gfx11(inst_.glc, inst_.dlc, inst_.slc);
  d->non_temporal = 0;
  d->wait_counter_type = amdgpu::WaitCounterType::LOADCNT;
  d->dst_reg_base = wf.vgpr_alloc().base + 0u + inst_.vdata;
  if (amdgpu::prepare_buffer_format(wf, *d, inst_.srsrc * 4, inst_.format, 4))
    mtbuf_calculate_addresses(inst_, wf, *d);
  set_data(std::move(d));
}

void TbufferStoreFormatXMtbuf::execute_impl(amdgpu::Wavefront &wf) {
  auto d = std::make_unique<amdgpu::VectorMemState>(amdgpu::GLOBAL_MEM);
  d->is_load = false;
  d->mtype = amdgpu::mtype_from_flags_gfx11(inst_.glc, inst_.dlc, inst_.slc);
  d->non_temporal = 0;
  d->wait_counter_type = amdgpu::WaitCounterType::STORECNT;
  if (amdgpu::prepare_buffer_format(wf, *d, inst_.srsrc * 4, inst_.format, 1))
    mtbuf_calculate_addresses(inst_, wf, *d);
  amdgpu::capture_buffer_format_store(wf, *d, wf.vgpr_alloc().base + 0u + inst_.vdata);
  set_data(std::move(d));
}

void TbufferStoreFormatXyMtbuf::execute_impl(amdgpu::Wavefront &wf) {
  auto d = std::make_unique<amdgpu::VectorMemState>(amdgpu::GLOBAL_MEM);
  d->is_load = false;
  d->mtype = amdgpu::mtype_from_flags_gfx11(inst_.glc, inst_.dlc, inst_.slc);
  d->non_temporal = 0;
  d->wait_counter_type = amdgpu::WaitCounterType::STORECNT;
  if (amdgpu::prepare_buffer_format(wf, *d, inst_.srsrc * 4, inst_.format, 2))
    mtbuf_calculate_addresses(inst_, wf, *d);
  amdgpu::capture_buffer_format_store(wf, *d, wf.vgpr_alloc().base + 0u + inst_.vdata);
  set_data(std::move(d));
}

void TbufferStoreFormatXyzMtbuf::execute_impl(amdgpu::Wavefront &wf) {
  auto d = std::make_unique<amdgpu::VectorMemState>(amdgpu::GLOBAL_MEM);
  d->is_load = false;
  d->mtype = amdgpu::mtype_from_flags_gfx11(inst_.glc, inst_.dlc, inst_.slc);
  d->non_temporal = 0;
  d->wait_counter_type = amdgpu::WaitCounterType::STORECNT;
  if (amdgpu::prepare_buffer_format(wf, *d, inst_.srsrc * 4, inst_.format, 3))
    mtbuf_calculate_addresses(inst_, wf, *d);
  amdgpu::capture_buffer_format_store(wf, *d, wf.vgpr_alloc().base + 0u + inst_.vdata);
  set_data(std::move(d));
}

void TbufferStoreFormatXyzwMtbuf::execute_impl(amdgpu::Wavefront &wf) {
  auto d = std::make_unique<amdgpu::VectorMemState>(amdgpu::GLOBAL_MEM);
  d->is_load = false;
  d->mtype = amdgpu::mtype_from_flags_gfx11(inst_.glc, inst_.dlc, inst_.slc);
  d->non_temporal = 0;
  d->wait_counter_type = amdgpu::WaitCounterType::STORECNT;
  if (amdgpu::prepare_buffer_format(wf, *d, inst_.srsrc * 4, inst_.format, 4))
    mtbuf_calculate_addresses(inst_, wf, *d);
  amdgpu::capture_buffer_format_store(wf, *d, wf.vgpr_alloc().base + 0u + inst_.vdata);
  set_data(std::move(d));
}

void TbufferLoadD16FormatXMtbuf::execute_impl(amdgpu::Wavefront &wf) {
  auto d = std::make_unique<amdgpu::VectorMemState>(amdgpu::GLOBAL_MEM);
  d->is_load = true;
  d->mtype = amdgpu::mtype_from_flags_gfx11(inst_.glc, inst_.dlc, inst_.slc);
  d->non_temporal = 0;
  d->wait_counter_type = amdgpu::WaitCounterType::LOADCNT;
  d->buffer_d16 = true;
  d->d16_hi = false;
  d->dst_reg_base = wf.vgpr_alloc().base + 0u + inst_.vdata;
  if (amdgpu::prepare_buffer_format(wf, *d, inst_.srsrc * 4, inst_.format, 1))
    mtbuf_calculate_addresses(inst_, wf, *d);
  set_data(std::move(d));
}

void TbufferLoadD16FormatXyMtbuf::execute_impl(amdgpu::Wavefront &wf) {
  auto d = std::make_unique<amdgpu::VectorMemState>(amdgpu::GLOBAL_MEM);
  d->is_load = true;
  d->mtype = amdgpu::mtype_from_flags_gfx11(inst_.glc, inst_.dlc, inst_.slc);
  d->non_temporal = 0;
  d->wait_counter_type = amdgpu::WaitCounterType::LOADCNT;
  d->buffer_d16 = true;
  d->d16_hi = false;
  d->dst_reg_base = wf.vgpr_alloc().base + 0u + inst_.vdata;
  if (amdgpu::prepare_buffer_format(wf, *d, inst_.srsrc * 4, inst_.format, 2))
    mtbuf_calculate_addresses(inst_, wf, *d);
  set_data(std::move(d));
}

void TbufferLoadD16FormatXyzMtbuf::execute_impl(amdgpu::Wavefront &wf) {
  auto d = std::make_unique<amdgpu::VectorMemState>(amdgpu::GLOBAL_MEM);
  d->is_load = true;
  d->mtype = amdgpu::mtype_from_flags_gfx11(inst_.glc, inst_.dlc, inst_.slc);
  d->non_temporal = 0;
  d->wait_counter_type = amdgpu::WaitCounterType::LOADCNT;
  d->buffer_d16 = true;
  d->d16_hi = false;
  d->dst_reg_base = wf.vgpr_alloc().base + 0u + inst_.vdata;
  if (amdgpu::prepare_buffer_format(wf, *d, inst_.srsrc * 4, inst_.format, 3))
    mtbuf_calculate_addresses(inst_, wf, *d);
  set_data(std::move(d));
}

void TbufferLoadD16FormatXyzwMtbuf::execute_impl(amdgpu::Wavefront &wf) {
  auto d = std::make_unique<amdgpu::VectorMemState>(amdgpu::GLOBAL_MEM);
  d->is_load = true;
  d->mtype = amdgpu::mtype_from_flags_gfx11(inst_.glc, inst_.dlc, inst_.slc);
  d->non_temporal = 0;
  d->wait_counter_type = amdgpu::WaitCounterType::LOADCNT;
  d->buffer_d16 = true;
  d->d16_hi = false;
  d->dst_reg_base = wf.vgpr_alloc().base + 0u + inst_.vdata;
  if (amdgpu::prepare_buffer_format(wf, *d, inst_.srsrc * 4, inst_.format, 4))
    mtbuf_calculate_addresses(inst_, wf, *d);
  set_data(std::move(d));
}

void TbufferStoreD16FormatXMtbuf::execute_impl(amdgpu::Wavefront &wf) {
  auto d = std::make_unique<amdgpu::VectorMemState>(amdgpu::GLOBAL_MEM);
  d->is_load = false;
  d->mtype = amdgpu::mtype_from_flags_gfx11(inst_.glc, inst_.dlc, inst_.slc);
  d->non_temporal = 0;
  d->wait_counter_type = amdgpu::WaitCounterType::STORECNT;
  d->buffer_d16 = true;
  d->d16_hi = false;
  if (amdgpu::prepare_buffer_format(wf, *d, inst_.srsrc * 4, inst_.format, 1))
    mtbuf_calculate_addresses(inst_, wf, *d);
  amdgpu::capture_buffer_format_store(wf, *d, wf.vgpr_alloc().base + 0u + inst_.vdata);
  set_data(std::move(d));
}

void TbufferStoreD16FormatXyMtbuf::execute_impl(amdgpu::Wavefront &wf) {
  auto d = std::make_unique<amdgpu::VectorMemState>(amdgpu::GLOBAL_MEM);
  d->is_load = false;
  d->mtype = amdgpu::mtype_from_flags_gfx11(inst_.glc, inst_.dlc, inst_.slc);
  d->non_temporal = 0;
  d->wait_counter_type = amdgpu::WaitCounterType::STORECNT;
  d->buffer_d16 = true;
  d->d16_hi = false;
  if (amdgpu::prepare_buffer_format(wf, *d, inst_.srsrc * 4, inst_.format, 2))
    mtbuf_calculate_addresses(inst_, wf, *d);
  amdgpu::capture_buffer_format_store(wf, *d, wf.vgpr_alloc().base + 0u + inst_.vdata);
  set_data(std::move(d));
}

void TbufferStoreD16FormatXyzMtbuf::execute_impl(amdgpu::Wavefront &wf) {
  auto d = std::make_unique<amdgpu::VectorMemState>(amdgpu::GLOBAL_MEM);
  d->is_load = false;
  d->mtype = amdgpu::mtype_from_flags_gfx11(inst_.glc, inst_.dlc, inst_.slc);
  d->non_temporal = 0;
  d->wait_counter_type = amdgpu::WaitCounterType::STORECNT;
  d->buffer_d16 = true;
  d->d16_hi = false;
  if (amdgpu::prepare_buffer_format(wf, *d, inst_.srsrc * 4, inst_.format, 3))
    mtbuf_calculate_addresses(inst_, wf, *d);
  amdgpu::capture_buffer_format_store(wf, *d, wf.vgpr_alloc().base + 0u + inst_.vdata);
  set_data(std::move(d));
}

void TbufferStoreD16FormatXyzwMtbuf::execute_impl(amdgpu::Wavefront &wf) {
  auto d = std::make_unique<amdgpu::VectorMemState>(amdgpu::GLOBAL_MEM);
  d->is_load = false;
  d->mtype = amdgpu::mtype_from_flags_gfx11(inst_.glc, inst_.dlc, inst_.slc);
  d->non_temporal = 0;
  d->wait_counter_type = amdgpu::WaitCounterType::STORECNT;
  d->buffer_d16 = true;
  d->d16_hi = false;
  if (amdgpu::prepare_buffer_format(wf, *d, inst_.srsrc * 4, inst_.format, 4))
    mtbuf_calculate_addresses(inst_, wf, *d);
  amdgpu::capture_buffer_format_store(wf, *d, wf.vgpr_alloc().base + 0u + inst_.vdata);
  set_data(std::move(d));
}

} // namespace rdna3
} // namespace rocjitsu
