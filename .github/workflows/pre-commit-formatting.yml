name: "Pre-Commit Formatting & Linting"
on:
  pull_request:

jobs:
  check-changes:
    runs-on: ubuntu-24.04
    outputs:
      rocprofiler-compute: ${{ steps.filter.outputs.rocprofiler-compute }}
      rocprofiler-sdk: ${{ steps.filter.outputs.rocprofiler-sdk }}
      rocprofiler-systems: ${{ steps.filter.outputs.rocprofiler-systems }}
    steps:
      - uses: actions/checkout@v6
      - id: filter
        uses: dorny/paths-filter@v4
        with:
          filters: |
            rocprofiler-compute:
              - 'projects/rocprofiler-compute/**'
            rocprofiler-sdk:
              - 'projects/rocprofiler-sdk/**'
            rocprofiler-systems:
              - 'projects/rocprofiler-systems/**'

  format-rocprofiler-compute:
    if: ${{ needs.check-changes.outputs.rocprofiler-compute == 'true' }}
    runs-on: ubuntu-24.04
    env:
      RUFF_VERSION: 0.14.11
      GERSEMI_VERSION: 0.22.1
      CLANG_FORMAT_VERSION: 22.1.0
    steps:
      - name: Checkout
        uses: actions/checkout@v6
        with:
          sparse-checkout: projects/rocprofiler-compute

      - name: Set up Python '3.x'
        uses: actions/setup-python@v5
        with:
          python-version: '3.x'

      - name: Install Dependencies
        working-directory: projects/rocprofiler-compute
        run: |
          python -m pip install --upgrade pip
          python -m pip install \
            ruff==${{ env.RUFF_VERSION }} \
            gersemi==${{ env.GERSEMI_VERSION }} \
            clang-format==${{ env.CLANG_FORMAT_VERSION }}

      - name: Ruff Formatting
        working-directory: projects/rocprofiler-compute
        run: |
          ruff check . --fix --exit-zero
          ruff format .
          git config --global user.name 'github-actions'
          git config --global user.email 'github-actions@github.com'
          git add -A .
          if ! git diff --cached --quiet; then
            echo "::error::Files were modified by ruff. Please run 'ruff check . --fix && ruff format .' locally and commit the changes."
            git diff --cached --patch # Show the diff in the logs
            exit 1
          else
            echo "Ruff found no issues or all issues were fixed and files are clean."
          fi

      - name: Gersemi Formatting
        working-directory: projects/rocprofiler-compute
        run: |
          set +e
          gersemi -i $(find . -type f | egrep 'CMakeLists.txt|\.cmake$')
          if [ $(git diff | wc -l) -gt 0 ]; then
            echo -e "\nError! CMake code not formatted. Run gersemi...\n"
            echo -e "\nFiles:\n"
            git diff --name-only
            echo -e "\nFull diff:\n"
            git diff
            exit 1
          fi

      - name: Clang Formatting
        working-directory: projects/rocprofiler-compute
        run: |
          set +e
          clang-format -i $(find src -type f | egrep '\.(h|hpp|hh|c|cc|cpp)(|\.in)$')
          if [ $(git diff | wc -l) -gt 0 ]; then
            echo -e "\nError! cxx code not formatted. Run clang-format...\n"
            echo -e "\nFiles:\n"
            git diff --name-only
            echo -e "\nFull diff:\n"
            git diff
            exit 1
          fi

      - name: Python Bytecode
        working-directory: projects/rocprofiler-compute
        run: |
          set +e
          FILES=$(find . -type f | egrep '__pycache__|\.pyc$')
          if [ -n "${FILES}" ]; then
            echo -e "\nError! Python bytecode included in commit\n"
            echo -e "### FILES: ###"
            echo -e "${FILES}"
            echo -e "##############"
            exit 1
          fi
