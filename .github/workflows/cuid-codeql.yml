name: "CodeQL Security Scan"

on:
  pull_request:
    branches: [develop]
    paths:
      - 'projects/cuid/**'
      - '.github/workflows/cuid-codeql.yml'
  schedule:
    - cron: '0 0 * * 1'
  workflow_dispatch:

permissions:
  contents: read

env:
  DEBIAN_FRONTEND: noninteractive
  DEBCONF_NONINTERACTIVE_SEEN: true
  BUILD_TYPE: Release
  ROCM_DIR: /opt/rocm

jobs:
  analyze:
    name: Analyze C/C++ Code
    if: ${{ github.event_name != 'pull_request' || !github.event.pull_request.head.repo.fork }}
    runs-on:
      - self-hosted
      - ${{ vars.RUNNER_TYPE }}
    container:
      image: ${{ vars.Ubuntu22_DOCKER_IMAGE }}
      options: --rm --privileged --device=/dev/kfd --device=/dev/dri --group-add video --cap-add=SYS_PTRACE --security-opt seccomp=unconfined --shm-size=64G --cap-add=SYS_MODULE -v /lib/modules:/lib/modules -u root
    permissions:
      actions: read
      contents: read
      security-events: write

    strategy:
      fail-fast: false
      matrix:
        language: ['cpp']

    steps:
      - name: Checkout repository
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2

      # AMD's custom query pack (amd-sit-sast-scv/cpp-queries) ships inside this bundle --
      # no GHCR login or token needed to pull it.
      - name: Download and extract CodeQL bundle
        shell: bash
        run: |
          curl -sS -o codeql-bundle-linux64.tar.gz \
            "https://mkmartifactory.amd.com:8443/artifactory/system-provisioning/CodeQL/bundles/latest/codeql-bundle-linux64.tar.gz"
          tar -xzf codeql-bundle-linux64.tar.gz
          chmod +x ./codeql/codeql
          ./codeql/codeql --version
      # Pack/suite version numbers change over time, so find the suite instead of hardcoding its path
      - name: Locate AMD custom query suite
        shell: bash
        run: |
          QLPACKS_ROOT=$(./codeql/codeql resolve qlpacks --format=json | jq -r '.[][]')
          AMD_SUITE=$(find $QLPACKS_ROOT -path "*/amd-sit-sast-scv/cpp-queries/*/codeql-suites/amd-cpp-windows-driver.qls" | head -1)
          echo "Using AMD suite: $AMD_SUITE"
          if [ -z "$AMD_SUITE" ]; then
            echo "Could not locate AMD custom query suite (amd-cpp-windows-driver.qls) under: $QLPACKS_ROOT" >&2
            exit 1
          fi
          echo "AMD_SUITE=$AMD_SUITE" >> "$GITHUB_ENV"
      # CodeQL's bundled autobuilder still auto-detects Makefile/CMake, same as the old Autobuild action
      - name: Create CodeQL database
        shell: bash
        run: |
          ./codeql/codeql database create codeqldb --language=${{ matrix.language }} --source-root=.

      - name: Build and Install OpenSSL 3.0 (if needed)
        run: |
          set -e
          OPENSSL_VER=$(openssl version | awk '{print $2}')
          echo "Current OpenSSL version: $OPENSSL_VER"
          if [[ "$OPENSSL_VER" != 3.* ]]; then
             echo "Installing OpenSSL 3.0..."
             if command -v apt-get &> /dev/null; then
                 apt-get update
                 apt-get install -y wget build-essential zlib1g-dev perl
             elif command -v dnf &> /dev/null; then
                 dnf install -y wget make gcc perl zlib-devel perl-core
             elif command -v zypper &> /dev/null; then
                 zypper install -y wget make gcc perl zlib-devel perl-core
             fi
             cd /tmp
             wget https://github.com/openssl/openssl/releases/download/openssl-3.0.12/openssl-3.0.12.tar.gz
             tar -xzvf openssl-3.0.12.tar.gz
             cd openssl-3.0.12
             ./config
             make -j $(nproc)
             make install
             ldconfig
             echo "OPENSSL_ROOT_DIR=/usr/local" >> $GITHUB_ENV
          else
             echo "OpenSSL 3.0+ already installed."
          fi

      - name: Build CUID
        run: |
          set -e
          BUILD_FOLDER=${{ env.PROJECT_DIR }}/build
          rm -rf $BUILD_FOLDER
          mkdir -p $BUILD_FOLDER
          cd $BUILD_FOLDER
          cmake ${{ env.PROJECT_DIR }} -DOPENSSL_ROOT_DIR=${{ env.OPENSSL_ROOT_DIR }}
          make -j $(nproc)

      - name: Perform CodeQL Analysis
        shell: bash
        run: |
          mkdir -p sarif-results
          ./codeql/codeql database analyze codeqldb \
            security-extended "$AMD_SUITE" \
            --format=sarifv2.1.0 \
            --sarif-category "/language:${{ matrix.language }}" \
            --output=sarif-results/cpp.sarif
      # Upload SARIF to GitHub Security > Code scanning alerts tab
      - name: Upload SARIF
        uses: github/codeql-action/upload-sarif@ff2f1c621b7f889edc0d3c761ac2e6a3f8cdb0dd # v4
        with:
          sarif_file: sarif-results/cpp.sarif
          category: "/language:${{ matrix.language }}"
