# Build products of remote/build.sh and remote/isa_check.sh. Regenerated by
# ./build.sh and ./isa_check.sh; nothing here is an input.
build/
isa/

# The PR link is derived from the branch and the commit message; regenerate it
# rather than keeping a stale copy under version control.
pr-link.txt
